// Register the `imageList` component on the `imageList` module,
angular
    .module('tagsSearch', [])
    .component('tagsSearch', {
            // Note: The URL is relative to our `index.html` file
            templateUrl: 'tags-search/tags-search.template.html',
            controller:
                function () {

                }
        }
    );


